import 'package:flutter/material.dart';

class EducationalResourcesPage extends StatelessWidget {
  const EducationalResourcesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Educational Resources')),
      body: Center(child: Text('Educational content about options trading')),
    );
  }
}
